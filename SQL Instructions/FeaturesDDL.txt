--TeamBigData.Utification.Features
CREATE TABLE dbo."Events"
(
userID int NOT NULL,
"disabled" int DEFAULT 0,
);

CREATE TABLE dbo.Pictures
(
userID int NOT NULL,
pinID int NOT NULL,
picData varbinary(max),
extension varchar(5),
"disabled" int DEFAULT 0,
CONSTRAINT Foreign_KEY_01 Foreign KEY (pinID) references dbo.Pins.pinID

);

CREATE TABLE dbo.Pins
(
pinID int NOT NULL Identity,
userID int NOT NULL,
lat varchar(50),
lng varchar(50),
pinType int,
"description" varchar(MAX),
"disabled" int DEFAULT 0,
completed int DEFAULT 0,
"dateTime" varchar(50),
);

CREATE TABLE dbo."Services"
(
userID int NOT NULL,
"disabled" int DEFAULT 0,
);